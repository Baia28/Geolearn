import sqlite3

conn = sqlite3.connect("database/content_poolbook.db")  # Update path if inside engine/ or database/
tables = conn.execute("SELECT name FROM sqlite_master WHERE type='table';").fetchall()
print("Actual Tables in DB:", tables)